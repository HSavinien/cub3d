/* ************************************************************************** */
/*                                                                            */
/*                                                        :::      ::::::::   */
/*   shoot.c                                            :+:      :+:    :+:   */
/*                                                    +:+ +:+         +:+     */
/*   By: cmaroude <marvin@42.fr>                    +#+  +:+       +#+        */
/*                                                +#+#+#+#+#+   +#+           */
/*   Created: 2023/01/19 10:47:56 by cmaroude          #+#    #+#             */
/*   Updated: 2023/01/20 15:35:25 by cmaroude         ###   ########.fr       */
/*                                                                            */
/* ************************************************************************** */

#include "cub3d.h"

/*void	get_weapon(t_mlx *mlx)
{
		search and load all images
	//use create_image or read_image
	mlx->weapon.disarmed.img_ptr = mlx_new_image(mlx->mlx_ptr,
	mlx->weapon.disarmed.width, mlx->weapon.disarmed.height,);
mlx->weapon.disarmed.data = 
(int *)mlx_get_data_addr(mlx->weapon.disarmed.img_ptr,
&mlx->weapon.disarmed.bpp, &mlx->weapon.disarmed.siz_l, 
&mlx->weapon.disarmed.endian);

}

t_img	get_weapon(t_img phase, t_mlx *mlx)
{
	t_img	shoot_img;

	shoot_img.img_ptr = 
}

void	do_shoot(t_mlx *mlx)
{
	t_img	shoot_img;

	get_weapon(mlx->weapon);
	shoot_img = mlx->weapon->armed_1;
	mlx_put_image_to_window(mlx->ptr, mlx->win_ptr, shoot_img,
		((WIN_W + shoot_img.width) / 2), (WIN_H - (shoot_img.height)));
	shoot_img = get_weapon(mlx->weapon->armed_2);
	mlx_put_image_to_window(mlx->ptr, mlx->win_ptr, shoot_img,
		((WIN_W + shoot_img.width) / 2), (WIN_H - (shoot_img.height)));
	shoot_img = get_weapon(mlx->weapon->armed_3);
	mlx_put_image_to_window(mlx->ptr, mlx->win_ptr, shoot_img,
		((WIN_W + shoot_img.width) / 2), (WIN_H - (shoot_img.height)));
	shoot_img = get_weapon(mlx->weapon->armed_4);
	mlx_put_image_to_window(mlx->ptr, mlx->win_ptr, shoot_img,
		((WIN_W + shoot_img.width) / 2), (WIN_H - (shoot_img.height)));
	shoot_img = get_weapon(mlx->weapon->disarmed);
	mlx_put_image_to_window(mlx->ptr, mlx->win_ptr, shoot_img,
		((WIN_W + shoot_img.width) / 2), (WIN_H - (shoot_img.height)));
}*/
